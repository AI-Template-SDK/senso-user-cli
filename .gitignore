# Build output (produced by `make build`, published from the npm tarball)
dist/

# Dependencies
node_modules/

# Test and coverage output
coverage/
.vitest/

# `npm pack` output, written by the smoke test
*.tgz

# Credentials and local environment. Never commit one; if you do, rotate it.
.env
.env.*
!.env.example

# Editor and OS noise
.DS_Store
*.swp
.idea/
.vscode/

# Logs
*.log
npm-debug.log*

# Agent skills installed into this working copy by `senso skills` (which shells
# out to shipables). A local artifact of running the CLI here, not source.
.claude/skills/
.cursor/skills/
